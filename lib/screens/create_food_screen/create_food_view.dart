import 'package:flutter/material.dart';

class CreateFoodView extends StatelessWidget {
  const CreateFoodView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Create food'));
  }
}
